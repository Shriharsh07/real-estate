{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/real-estate-app/browser",
  "builds": [
    {
      "src": "server-go/main.go",
      "use": "@vercel/go"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
